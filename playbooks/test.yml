---
#- hosts: velociraptor
#  tasks:
#    - fetch:
#        src: /opt/velociraptor/etc/client.config.yaml
#        dest: "{{ inventory_dir | dirname }}/roles/velociraptor/files/"
#        flat: true

- hosts: all
  roles:
     - domain_controller
